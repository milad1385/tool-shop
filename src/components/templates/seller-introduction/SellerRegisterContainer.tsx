import React from "react";

function SellerRegisterContainer({ children }: { children: React.ReactNode }) {
  return <div className="flex-1 md:flex-[0.8] md:px-10 lg:px-56 py-2">{children}</div>;
}

export default SellerRegisterContainer;
